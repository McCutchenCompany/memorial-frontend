import { findUserPositionReducer, INITIAL_STATE } from './find-user-position.reducer';

describe('findUserPositionReducer', () => {
  // it('should return initial state', () => {
  //   expect(findUserPositionReducer(undefined, {type: ''})).toBe(INITIAL_STATE);
  // });
});
